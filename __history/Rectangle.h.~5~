#ifndef RECTANGLE_H
#define RECTANGLE_H

class Rectangle{
	public:
	protected:
		virtual double area();
		virtual double perimeter();
		static bool setParams(std::string str,Shape& circle);
	private:
		double base;
		double length;
		double height;
		double radius;
};

#endif
