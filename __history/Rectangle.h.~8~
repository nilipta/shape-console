#ifndef RECTANGLE_H
#define RECTANGLE_H

class Rectangle{
	public:
		Rectangle(std::string strLine);
		virtual double area();
		virtual double perimeter();
		static bool setParams(std::string str,Shape& rect);
	private:
		double base;
		double length;
		double height;
		double radius;
};

#endif
